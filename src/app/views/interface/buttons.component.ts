import { Component } from '@angular/core';

@Component({
    selector: 'interface-buttons',
    template: require('./buttons.template.html')
})

export class ButtonsComponent{

    constructor() {
    }

}
